# Python bytecode and cache files
__pycache__/
*.pyc

# Pixi stuff
.pixi/
# Vendored fractal-web clone (regenerated via the `clone-fractal-web` pixi task).
fractal-web-clone/
src/frontend/node_modules/
src/frontend/build/
src/frontend/.svelte-kit/

# Generated files
state.json
*.zarr
.cache/

# Frctal Lite project files
*.flp
# MacOS metadata files
.DS_Store

# pyinstaller files
build/
dist/
fractal.spec

# Electron (v3 branch) — the electron/ subfolder holds the npm/electron-builder
# tooling; node_modules/, out/ and dist-electron/ are unanchored patterns so
# they also match there.
node_modules/
out/
dist-electron/
electron/resources/fractal-app-lite/
