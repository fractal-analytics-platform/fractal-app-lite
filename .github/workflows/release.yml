name: Create GitHub release
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+[a-c][0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+-[a-c][0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+alpha[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+beta[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+rc[0-9]+'

permissions: {}


jobs:
  create-release-draft:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
      - run: gh release create "${GH_REF_NAME}" --draft --title "${GH_REF_NAME}"
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REF_NAME: ${{ github.ref_name }}

  build:
    runs-on: ${{ matrix.os }}
    needs: create-release-draft
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - uses: prefix-dev/setup-pixi@5185adfbffb4bd703da3010310260805d89ebb11

      - name: Build fractal-web components
        run: |
          pixi run clone-fractal-web
          cd fractal-web-clone/components && npm install --omit=peer

      - name: Build frontend
        run: pixi run build-frontend

      - name: Build executable
        run: pixi run build-executable

      - name: Rename artifact - Linux
        run: mv dist/fractal "dist/fractal-${{ github.ref_name }}-linux"
        if: runner.os == 'Linux'

      - name: Rename artifact - macOS
        run: mv dist/fractal "dist/fractal-${{ github.ref_name }}-mac"
        if: runner.os == 'macOS'

      - name: Rename artifact - Windows
        run: mv dist/fractal.exe "dist/fractal-${{ github.ref_name }}.exe"
        if: runner.os == 'Windows'

      - run: gh release upload "${{ github.ref_name }}" "dist/fractal-${{ github.ref_name }}*"
        env:
          GH_TOKEN: ${{ github.token }}

  publish-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    needs: [build]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
      - run: gh release edit "${GH_REF_NAME}" --draft=false
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REF_NAME: ${{ github.ref_name }}
